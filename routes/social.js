var express = require('express');
var router = express.Router();
var socialDao = require('../dao/social-dao');

///**
// * 登录用户
// */
//router.post("/loginUser", function (req, res, next) {
//    socialDao.loginUser(req, res, next);
//});

module.exports = router;